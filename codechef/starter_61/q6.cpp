#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin>>t;
    while(t--) {
        int n;
        cin>>n;
        vector<int> red;
        vector<int> blue;
        for(int i=0;i<n;i++) {
            int k;
            cin>>k;
            red.push_back(k);
        }
        for(int i=0;i<n;i++) {
            int k;
            cin>>k;
            blue.push_back(k);
        }
        
    }
}